# vim: ft=swayconfig

set $left h
set $down j
set $up k
set $right l

set $term foot
set $menu dms ipc call spotlight toggle

### Session startup
#
exec env QT_QPA_PLATFORM=wayland dms run --session
exec wl-paste --watch cliphist store

#
# include /etc/scroll/config.d/*
